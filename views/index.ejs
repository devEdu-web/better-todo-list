<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link href="/css/style.css" rel="stylesheet">
    <title>Tasks</title>
</head>
<body>
    
        
    <main>

        <div class="mb-5">

            <form action="/add-task" method="post" class="form">
                <input type="text" name="task" id="task" placeholder="Add a new task">
                <select name="priority" class="form-select">
                    <option selected>Priority</option>
                    <option value="low">Low</option>
                    <option value="normal">Normal</option>
                    <option value="important">Important</option>
                    <option value="critical">Critical</option>
                </select>
                <input type="submit" value="Add Task">
            </form>

        </div>

        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Tasks</th>
                        <th>Priority</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    
                    <% if(tasks){ %>


                        <% tasks.tasks.forEach((task, index) => { %> 
                            <tr>
                                <th><%= index+1 %> </th>
                                <td id="className"><%= task.task %></td>
                                <td id="classPriority"><%= task.priority %></td>
                                <td class="d-flex justify-content-center"><button type="button" class="btn-close" aria-label="Close"></button></td>
                            </tr>
    
                        <% }) %> 


                    <% } else{ %>  
                        <p>no photos uploaded</p>
                    <% } %>

                </tbody>
            </table>
        </div>

        <div class="user">
            <span>Welcome, <%= userName %>! </span>
            <a href="/logout" class="logout">Logout</a>
        </div>

    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script src="/js/deleteTask.js"></script>
</body>
</html>